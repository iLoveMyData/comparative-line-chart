# Build artefacts
.tmp/
dist/
node_modules/
*.pbiviz
webpack.statistics*.html

# Marketplace render tooling is local-only (SVG + PNG outputs are committed,
# not the npm helper used to rasterize them)
assets/marketplace/package.json
assets/marketplace/package-lock.json

# Raw high-resolution captures + legacy SVG mockups — kept locally for
# regeneration, not committed (the final 1366x768 PNGs are the shipped assets)
assets/marketplace/screenshot-raw-*.png
assets/marketplace/screenshot-1.svg
assets/marketplace/screenshot-2.svg
assets/marketplace/build-screenshots.mjs

# Local state
.vscode/
.idea/
.DS_Store
Thumbs.db
*.log

# pbiviz certificate (local, never committed)
*.pfx
*.key

# Environment
.env
.env.local
